export interface Product {
  id: string;
  name: string;
  category: string;
  price: string;
  dosage: string;
  image: string;
  link: string;
}

export interface Stat {
  value: string;
  label: string;
}
